name: Integration

on:
  workflow_call:
  workflow_dispatch:

jobs:
  action:
    name: Action
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run action
        uses: ./
        with:
          script: ./cli.ts
          assets: README.md LICENSE
          options: --dry-run --draft
